{
  "name": "resource-scheduler-backend",
  "version": "1.0.0",
  "description": "Conflict-aware resource allocation API using weighted interval scheduling (DP + binary search).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/",
    "reset-db": "node -e \"require('fs').writeFileSync('./data/db.json', JSON.stringify({resources:[],requests:[],allocations:[]}, null, 2))\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
